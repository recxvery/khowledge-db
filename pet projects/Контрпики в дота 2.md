Отлично! Вот **пошаговый план обучения с фокусом на понимании**, а не просто копировании кода:

---

### **1. Разберись с API (без кода)**

- **Изучи документацию:**  
    Открой [OpenDota API Docs](https://docs.opendota.com/) → найди раздел `Heroes` → посмотри, какие есть endpoints (например, `/heroes/{id}/matchups`).  
    **Задача:** Напиши в тетради:
    
    - Какой URL даст тебе список всех героев?
        
    - Как получить контрипики для Pudge (id=14)?
        
- **Потренируйся вручную:**  
    Открой в браузере `https://api.opendota.com/api/heroes` — увидишь JSON со всеми героями.  
    **Вопрос:** Какое поле в этом JSON содержит имя героя?
    

---

### **2. Подготовка проекта**

1. Создай папку проекта:
    
    bash
    
    mkdir dota-counters && cd dota-counters
    go mod init github.com/твой-ник/dota-counters
    
2. Установи библиотеки для HTTP-запросов:
    
    bash
    
    go get net/http
    go get encoding/json
    

---

### **3. Начни с малого (без героев)**

**Задача 1:** Напиши программу, которая:

- Делает GET-запрос к `https://api.opendota.com/api/status`
    
- Выводит статус ответа (200, 404 и т.д.)
    
- Читает тело ответа и печатает его
    

**Подсказки:**

- Используй `http.Get(url)`
    
- Для чтения тела: `ioutil.ReadAll(resp.Body)`
    
- Не забудь `resp.Body.Close()`
    

---

### **4. Парсинг JSON**

**Задача 2:** Усложни предыдущую программу:

1. Получи список героев с `https://api.opendota.com/api/heroes`
    
2. Распарсь JSON в структуру:
    
    go
    
    type Hero struct {
        ID   int    `json:"id"`
        Name string `json:"localized_name"`
    }
    
3. Выведи только имена 10 первых героев.
    

**Как учиться:**

- Гугли: "Go parse JSON example"
    
- Если застрял — посмотри документацию `encoding/json`
    

---

### **5. Получение контрипиков**

**Задача 3:**

1. Попроси пользователя ввести имя героя (как в твоей старой программе).
    
2. Найди его ID в списке героев.
    
3. Сделай запрос к `/heroes/{id}/matchups`
    
4. Выведи топ-3 худших матчапов (сортировка по `wins`).
    

**Совет:**  
Сначала попробуй получить данные для конкрентного ID без ввода пользователя (например, Pudge — ID 14).

---

### **6. Дополнительно (если захочешь)**

- Кэширование: сохраняй данные в файл, чтобы не делать запросы каждый раз.
    
- Интерфейс: сделай простой веб-сервер на Go (вывод в браузере).
    
- Тесты: напиши тест для проверки парсинга JSON.
    

---

### **Как действовать:**

1. Делай **по одному шагу** из списка.
    
2. Сталкиваешься с ошибкой → гугли её + читай документацию.
    
3. Не получается после 30 минут → спроси меня (объясню принцип, а не решение).
    

**Важно:** Не пытайся сделать всё сразу. Лучше работающий кусочек, чем "идеальный" незавершённый проект.

---

### **Полезные ссылки:**

- [Документация OpenDota API](https://docs.opendota.com/)
    
- [Работа с HTTP в Go](https://gobyexample.com/http-clients)
    
- [Парсинг JSON](https://gobyexample.com/json)
    

Когда сделаешь шаг 3 — пиши, обсудим следующий! 💪



## По пути
```go
package main



import (
	"fmt"
	"io"
	"log"
	"net/http"
	"encoding/json"
)

type Hero struct {
	ID int `json:"id"`
	Name string `json:"localized_name"`
	PrimaryAtrribute string `json:"primary_attr"`
	Roles []string `json:"roles"`
}


func main() {
	// using open dota api as url 
	
	url := "https://api.opendota.com/api/heroes"
	
	resp, err := http.Get(url)
	if err != nil {
		log.Fatalf("Request error %v", err)
	}
	
	defer resp.Body.Close()
	
	
	//проверяем статус ответа. 200 = ОК
	if resp.StatusCode != http.StatusOK {
		log.Fatalf("API вернуло статус: %s", resp.Status)
	}
	
	body, err := io.ReadAll(resp.Body)
	if err != nil {
		log.Fatalf("Ошибка чтения тела ответа: %v", err)
	}
	
	fmt.Println("===Pure JSON answer ===")	
	fmt.Println(string(body))//api answer
	
	// Подготовка к парсингу JSON (следующий шаг)
	fmt.Println("\nГотово к парсингу JSON!")
	var heroes []Hero 
	err = json.Unmarshal(body, &heroes)
	for i, hero := range heroes {
		fmt.Printf("Герой: %d\n", i+1)
		fmt.Printf("ID: %v\n", hero.ID)
		fmt.Printf("Имя: %s\n", hero.Name)
		fmt.Printf("Роли: %v\n ", hero.Roles)
	}
}

```

Итак разберём что да как. Я просто беру, вставляю url нужного нам апишника по доте, далее в переменную resp получаем ответ с сервера. resp.status очевидно статус ответа, в resp.body весь сырой JSON который мы потом закрываем через defer. через ReadALl переводим тело ответа в биты, а потом их же конвертируем в строки. Далее просто объявляем срез массивов элементы которого будут построены по структуре Hero.