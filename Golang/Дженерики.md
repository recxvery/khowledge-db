**Дженерики** - механизм обобщённого программирования, позволяющий создавать функции, типы и структуры, которые работают с различными типами данных, сохраняя тип безопасность на уровне компиляции

		#### Type parametres

```go
func sum[V int64 | float64](numbers []V) V {
	var sum V
	for _, num := range numbers {
		sum += num
	}

	return sum
}
```
Пример классического `Generic'a` 

```go
[V int64 | float64] = эта строка называется type parametres
```

Несмотря на то что `V - пустой интерфейс по сути` а точнее `параметр типа`, раз мы указали параметры типов мы не сможем использовать любой другой тип кроме констрант `int64/float64`.

#### Comparable/any

```go
func contains[T comparable](elements []T, searchEL T) bool {
	for _, el := range elements {
		if searchEL == el {
			return true
		}
	}

	return false
}
```
Ещё одна фича, что у нас есть `comparable/uncomparable data types` в GO. Так, что в эту функцию можно будет запихнуть все типы кроме мапы, слайсов и структур содержащих мапу/слайсы в полях

```go
func show[T any] (entities ...T) {
	fmt.Println(entities)
}
```
Ещё один специфичный параметр в generic-функциях. `...T` - означает грубо говоря распаковку или или же `variadic parametr` - произвольное количество параметров\

#### Обобщённые типы

```go
type Number interface {
	int64 | float64
}

type Numbers[T Number] []T
```
Мы можем создавать кастомные типы. Здесь например видны что к `Numbers` массиву будут относиться типы данных удовлетворяющие интерфейсы `Number`, то есть `int64/float64`. 

Вот в этой функции мы и создаём **обобщённые типы**:

```go
func unionInterfaceAndType() {
	var ints Numbers[int64]
	ints = append(ints, []int64{1, 2, 3, 4, 5, 6}...)

	floats := Numbers[float64]{1.0, 2, 5, 6, 5	}

	fmt.Println(unionSumInterface(ints))
	fmt.Println(unionSumInterface(floats))
}
```

И в функции `unionSumInterface` вместо указания `int64 | float64` будет просто:
```go
func unionSumInterface[V Number](numbers []V) V {
	var sum V
	for  _, num := range numbers {
		sum += num
	}
	return sum
}
```

#### Type approximation 
У нас есть вот такая функция 
```go
func typeApproximation() {
	customInts := []CustomInt{1, 2, 3, 4, 5, 6}
	castedInts := make([]int64, len(customInts))

	for idx, val := range customInts {
		castedInts[idx] = int64(val)
	}

	fmt.Println(unionSumInterface(customInts))
	fmt.Println(unionSumInterface(castedInts))
 }
```

В функции `unionSumInterface`:
```go
func unionSumInterface[V Number](numbers []V) V {
	var sum V
	for  _, num := range numbers {
		sum += num
	}
	return sum
}
```
И нам закономерно будет выдавать ошибку. Что с этим делать? 

```go
type Number interface {
	~int64 | float64
}

type Numbers[T Number] []T

type CustomInt int64 

func (ci CustomInt) isPositive() bool {
	return ci > 0
}
	 
```
Тут всё решает `~` которая говорит компилятору принимать все обобщённые типы основанные на `int64`. Это и называется `type assertion`

ну или вот так ммне подсказала ИИшка: 
Символ `~` указывает, что constraint принимает не только сам тип, но и все пользовательские типы, чей **underlying type** совпадает с указанным.


Задача с который у меня дико сгорела жопа:

```go
type Stack[T any] struct {
	data []T
}

func (s *Stack[T]) Push(elem T) {
	s.data = append(s.data, elem)
}

func (s *Stack[T]) Pop() (T, bool) {
	var zero T

	if len(s.data) == 0 {
		return zero, false
	}

	last := s.data[len(s.data)-1]
	s.data = s.data[:len(s.data)-1]
	return last, true
}

func (s Stack[T]) Len() int {
	return len(s.data)
}

```

## 🔴 Задача 5 — дженерики + поведение (самая важная)

**Условие:**  
Напиши функцию `Filter`, которая:

- принимает слайс любого типа
    
- принимает функцию-предикат
    
- возвращает новый слайс с отфильтрованными элементами
    

**Сигнатура (почти):**

`func Filter[T any](items []T, fn func(T) bool) []T`

**Подсказка:**

- дженерики **не решают логику**
    
- поведение передаётся явно
    

**Проверка себя:**

`Filter([]int{1, 2, 3, 4}, func(v int) bool { 	return v%2 == 0 }) // [2 4]`