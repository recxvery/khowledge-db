  **Модуль** - это какой-то проект, где есть главный файл и какие-либо папки.

![[Pasted image 20251005201807.png]]
схема модуля и билиотека

**Библиотека** - грубо говоря наборы просто заготовленных файлов, которые мы используем в собственных проектах. По сути это тоже модуль.

![[Pasted image 20251005202308.png]]
На этой схеме в красной рамке выделены пакеты. Грубо говоря это просто папки в модуле с заготовленными файлами. Пакет - папка с гошными файлами. 

![[Pasted image 20251005202515.png]]
Пакеты ещё могут быть вложенными, как представлено здесь.

```go
go mod init

```
создаём модуль в проекте.

вот так примерно выглядит структура модуля, где у нас пакет greeting. В пакете у нас есть файл с функцией SayHello().

```go
package greeting

import "fmt"

func SayHello() {
	fmt.Println("Hello Golang packages")
}
```

![[Pasted image 20251005203434.png]]

Вывод в терминале:
```
PS C:\Users\hs-ha\Desktop\For Study\backend\GOlang\packages_modules> go run main.go
Hello world
Hello Golang packages
```

Во всех файлах внутри пакета должно быть обязательно одно и то же название, иначе GO не понимает почему у нас в одной папке два разных пакета, и не один из файлов ни будет работать. При чём название пакета как я понял не имеет значения. То есть пакет можно называть не только названием папки.

Как удобство ещё могу отметить, что файлы одного пакета видят функции друг-друга. То есть спокойно можно сделать вот так
```go
package greeting

func GiveMeInt() int {
	return 55
}
```
int.go

```go
package greeting    

import "fmt"

func SayHello() {
	fmt.Println("Hello Golang packages")

	j := GiveMeInt() + 1
	fmt.Println("From SayHello:", j)
}
```
greeting.go 

Bывод с **main.go**:
```go
package main

import (
	"fmt"
	"study/greeting"
)

func main() {
	fmt.Println("Hello world")

	greeting.SayHello()
	greeting.SayBad()
}
```

```go
go run main.go 
Hello world
Hello Golang packages
From SayHello: 56
```

---
**Важная деталь - название функций в пакетах, как и в случае со структурами нужно указывать с большой буквы иначе они не будут публичными, GO просто не увидит функции**

## Инкапсуляция - одно из главных преимуществ использований пакетов

```go 
package user

type User struct {
	name string
	age  int
}

func NewUser(name string, age int) User {
	if name == "" {
		return User{}
	}

	if age <= 0 || age > 150 {
		return User{}
	}

	return User{
		name: name,
		age:  age,
	}
}

func (u *User) SetNewName(name string) {
	if name != "" {
		u.name = name
	}
}

func (u *User) SetNewAge(age int) {
	if age >= 0 || age < 150 {
		u.age = age
	}
}

func (u *User) GetName() string {
	return u.name
}

func (u *User) GetAge() int {
	return u.age
}
```
Вот наш файл находящийся в директории study/user

Вот что в файле main.go:
```go
package main

import (
	"fmt"
	"study/user"
)

func main() {
	u := user.NewUser("danil", 50)

	fmt.Println(u.GetAge())
}

```

В чём прикол? В том что если у нас будет на коллеге идиот, который захочет сломать нашу структуру, выставить неправильный возраст/пустое имя он не сможет это сделать, т.к мы задали ограничения, а поля структуры не публичны. Соответственно единственным что он может сделать, создать структуру изменить её данные в условиях ограничений, получить имя/возраст - **В С Ё**. И это и есть инкапсуляция и это классно. Если же мы просто укажем публичные поля в структуре то GO, просто забъёт хуй на это и присвоит значение напрямую без лишних проверок. Спасибо легенда. В целом этот пример - **классический пример
инкапсуляции в ООП**


---
```go
go mod tidy
```
Зачем нужна эта команда? Короче, она делает следующее(настраивает зависимости в нашем файле). Если библиотеки просто не будет в нашем файле то команда удалит её из go.mod. Он также добавляет используемые контрольные суммы(хэши) в файл.  Также мы можем перезаписать версию в go.mod тогда версия измениться и в go.mod + контрольные суммы в go.sum.

> [!NOTE] Классная библиотека для красивого вывода
> https://github.com/k0kubun/pp





![profile](https://assets.alicdn.com/g/qwenweb/qwen-webui-fe/0.0.222/static/qwen_icon_dark_84.png)

Qwen3-Max

Отлично! Учитывая, что ты уже знаком с базовыми структурами данных Go (массивы, слайсы, мапы, структуры), вот **5 задач на работу с пакетами и модулями**, упорядоченных **от простой к сложной**. Они помогут тебе освоить организацию кода, импорт, экспорт, управление зависимостями и создание собственных модулей.

---

### 🔹 **Задача 1 (Начальный уровень)**

**Создание локального пакета и его использование**

**Цель**: Научиться создавать и использовать собственный пакет внутри одного модуля.

**Задание**:

1. Создай модуль `myapp` (`go mod init myapp`).
2. Внутри создай папку `mathutils`.
3. В `mathutils` создай файл `math.go`, в котором определи **экспортируемую** функцию `MaxInt(a, b int) int`, возвращающую большее из двух чисел.
4. В корне модуля создай `main.go`, который импортирует `myapp/mathutils` и использует `MaxInt` для сравнения двух чисел.

**Проверка**: Программа должна компилироваться и выводить правильный результат.

---

### 🔹 **Задача 2 (Базовый уровень)**

**Работа с несколькими файлами в одном пакете**

**Цель**: Понять, как Go объединяет несколько файлов в один пакет.

**Задание**:

1. В пакете `mathutils` из задачи 1 добавь второй файл `min.go`.
2. В нём определи функцию `MinInt(a, b int) int`.
3. Обнови `main.go`, чтобы он использовал **обе** функции: `MaxInt` и `MinInt`.

**Проверка**: Убедись, что обе функции доступны без дополнительных импортов — они в одном пакете.

---

### 🔹 **Задача 3 (Средний уровень)**

**Создание пакета с внутренними (неэкспортируемыми) функциями и структурами**

**Цель**: Научиться использовать неэкспортируемые элементы внутри пакета.

**Задание**:

1. Создай пакет `userdb` в своём модуле.
2. Внутри определи **неэкспортируемую** структуру `user` с полями `name` и `email` (строчные буквы!).
3. Создай **экспортируемую** функцию `NewUser(name, email string) *User` (обрати внимание: возвращаемый тип — `*User`, но структура называется `user` — это невозможно! Поэтому: либо сделай структуру `User`, либо верни интерфейс/используй фабрику с приватной структурой).  
    → **Подсказка**: сделай структуру `user` приватной, но функцию `NewUser` возвращает `*user`, но тогда она не экспортируется. Лучше: сделай структуру `User` с **экспортируемыми полями**, но добавь **приватную вспомогательную функцию** `validateEmail(email string) bool`, которую используешь внутри `NewUser`.
4. В `main.go` создай пользователя и выведи его данные.

**Проверка**: Убедись, что `validateEmail` не видна извне пакета, но используется внутри.

---

### 🔹 **Задача 4 (Продвинутый уровень)**

**Использование внешнего модуля через `go get`**

**Цель**: Освоить работу с публичными модулями из интернета.

**Задание**:

1. Добавь в свой проект зависимость от популярного модуля, например `github.com/google/uuid`.
2. Создай пакет `idgen`, в котором будет функция `GenerateID() string`, возвращающая новый UUID.
3. Используй эту функцию в `main.go`.

**Команды**:

bash

1

go get github.com/google/uuid

**Проверка**: Программа должна генерировать и выводить валидный UUID.

---

### 🔴 **Задача 5 (Экспертный уровень)**

**Создание собственного переиспользуемого модуля и публикация его "виртуально" (локально)**

**Цель**: Понять, как создаются и используются независимые модули.

**Задание**:

1. Создай **новый модуль** `mylib` в отдельной папке:
    
    bash
    
    1
    
    2
    
    mkdir mylib && cd mylib
    
    go mod init github.com/yourname/mylib
    
2. В нём создай пакет `stats` с функцией `Average(nums []float64) float64`.
3. Вернись в основной проект (`myapp`) и добавь **локальную замену** (replace directive), чтобы использовать `mylib` как зависимость **без публикации в интернете**.  
    В `go.mod` основного проекта добавь:
    
    go
    
    1
    
    2
    
    3
    
    require github.com/yourname/mylib v0.0.0
    
      
    
    replace github.com/yourname/mylib => ../mylib
    
4. Используй `mylib/stats.Average` в `main.go`.

**Проверка**: Программа должна работать, используя твой локальный модуль как внешнюю зависимость.

---

💡 **Совет**:  
После выполнения всех задач у тебя будет чёткое понимание:

- как организовывать код в пакеты,
- как управлять видимостью,
- как подключать внешние и локальные зависимости,
- как создавать собственные переиспользуемые модули.

Удачи в практике! 🚀