[[map'ы]]
Типичное создание массива:

```Go
package main

import "fmt"

func main() {
	transactions := [3]int{5, 10, -6}
	bank_transactions := [3]string{"СБЕР", "Альфа Банк", "Т-Банк"}
	fmt.Println(transactions)
	fmt.Println(bank_transactions)
}
```

  
Также можно просто создать пустой массив и тогда выведутся просто дефолтные задачи по типу [0 0 0].  

  

что ещё интересного так это то что мы не можем скопировать один массив в другой т.к. они будут отличаться по размеру и будет ошибка **MISMATCH TYPE**

Также стоит отметить что массивы это не тоже самое что списки в Питоне, то есть именно массивы копируются нессылочно и если изменить один массив, то второму будет похуй и он останется таким же. Кст их не оч используются из-за того что их заданный размер это неудобно

  

![[image 11.png|image 11.png]]

  

  

Короче, как я понял ну или я так и не понял. Есть срезы и это типо отдельный(или нет вид массивов) и суть их в том что в отличие от массива, как раз таки они тупо как и в Питоне являются ссылками друг на дружку

![[image 1 2.png|image 1 2.png]]

  

## Все различия:

![[image 2 2.png|image 2 2.png]]

pseudo-constants - это неизменяемые переменные(как массив), которые при этом не являются константами. строка кстати точно такая же.

  

Задаём -

```Go
	var w = [...]int{1, 2, 3} //array
	var x = []int{0,0,0} //slice
```

  

## Slice


![[Pasted image 20250703203008.png]]
Наглядный пример отличия одного от другого.



подSlice это по сути окошко к массиву. По крайней мере как в данном случае где "[1:]" мы просто ссылаемся на массив. Поэтому он очень легковесный и это ведущий тип данных. Меняем slice - меняем исходный массив

```Go
	transaction := [5]int{5, 10, -7, 4, 5}
	transactionPartial := transaction[1:]
	transactionPartial[0] = 30

	fmt.Println(transaction) //[5 30 -7 4 5]
	fmt.Println(transactionPartial) //[30 -7 4 5]
```
Наглядный пример

Вот ещё наглядный пример, что **подslice** по сути ссылается даже друг на друга, ну и на оригинальный массив

```go
	transaction := [5]int{5, 10, -7, 4, 5}
	transactionPartial := transaction[1:]
	transactionNewPartial := transactionPartial[:1]
	transactionNewPartial[0] = 780


	fmt.Println(transactionNewPartial)//[780]
	fmt.Println(transactionPartial)//[780 -7 4 5]
	fmt.Println(transaction)//[5 780 -7 4 5]
```

  



## len / capacity

**len** - по сути просто длина(кол-во элементов среза/массива, которое мы итак видим)

**Capacity(вместимость)** - по сути, сколько ещё элементов мы можем вместить исходя из основного массива. 

Пример: в основном массиве 6 элементов. Мы решили взять 4 элемента(с первого по пятый), **len** будет 4, как бы здесь ничего удивительного, а **capacity** будет уже 5. Потому что из оригинального массива, мы не забрали последний элемент и срез может вместить ещё один. **Почему не capacity не равно 6?**. Потому что засчитываются только элементы справа.
Код:

```go
	transaction := [6]int{5, 10, -7, 4, 5, 6}
	transactionPartial := transaction[1:5]
	transactionNewPartial := transactionPartial[:1]
	transactionNewPartial[0] = 780


	fmt.Println(len(transactionNewPartial), cap(transactionNewPartial))//1 5
	fmt.Println(len(transactionPartial), cap(transactionPartial))//4 5
	fmt.Println(len(transaction), cap(transaction))//6 6
```

  ![[Pasted image 20250703202107.png]]
то есть опять же capacity определяет именно сколько элементов справа. на этом слайде 0 элемент игнорируется двумя срезами

Так вот взглянем, на ещё один не менее интересный случай:
```go
func main() {
	transaction := [6]int{5, 10, -7, 4, 5, 6}
	transactionPartial := transaction[1:5]
	transactionNewPartial := transactionPartial[:1]

	fmt.Println(transaction)//[5 10 -7 4 5 6]

	transactionNewPartial[0] = 780
	fmt.Println(transactionNewPartial)//[789]

	transactionNewPartial = transactionNewPartial[0:5]
	fmt.Println(transactionNewPartial)//[780 -7 4 5 6]
}
```
Capacity, помогает нам сдвигаться и захватывать и другие элементы тоже. Выглядит очень странно, типо мы расширяем оригинальный срез с одного элемента до пяти. Но если помнить, что всё это это просто ссылки, то становится легче.

###### **К слову если длина увеличивается и превышает ёмкость то массив:**
- Go создаёт новый массив большего размера
- Все элемента из старого массива копируются в новый
- Сapacity увеличивается и внутренняя ссылка из старого массива изменяется на новый массив с большим размером.
Всё это называется - **реалокацией(reallocation)**. Она позволяет срезу расти динамически.
```go
	tr[0] = 50
	tr[1] = 69
	fmt.Println(tr, cap(tr))

	tr = append(tr, 6)
	fmt.Println(tr, cap(tr)) //[50 69 6] 3

	tr = append(tr, 6)
	fmt.Println(tr, cap(tr))// [50 69 6 6] 6
```
Наглядный пример реалокации. при последнем изменении массива вместимость увеличилась вдвое.

###### **Конкретный алгоритм реалокации:**
1. Мы инициализировали слайс с длиной `2`, и ёмкостью `3` .![[Pasted image 20250703205539.png]]
2. Мы добавили новый третий элемент — длина все еще **меньше или равна** вместимости, поэтому реалокация пока **не происходит**.![[Pasted image 20250703205559.png]]
3. Бум 🆘🆘🆘 !!! Длина превысила ёмкость!!! Срочно провести реалокацию! Создать новый массив который больше предыдущего, скопировать все элементы в этот массив и изменить указатель из старого массива на новый.![[Pasted image 20250703205615.png]]


## Про append()

```Go
package main

import "fmt"

func main() {
	eptScoresTop := []int{17605, 12500, 10670}
	slice := eptScoresTop
	eptScoresTop = append(eptScoresTop, 10185)
	fmt.Println(eptScoresTop)
	fmt.Println(slice)
}
```

короче append создаёт по сути уже другой участок памяти и так мы можем спокойно менять динамические массивы(slice) так что условно как тут показано slice будет ссылаться на другие данные в памяти, а не на новые.

## Make

честно, я пока так и не понял зачем эта поебота нужна. то есть тут типо задаётся минимальный capacity для среза и максимальный. при этом максимум нарушается и хз эм….

```Go
	tr := make([]string, 0, 2)
	tr = append(tr, "a") // 1 2
	fmt.Println(len(tr), cap(tr))
	tr = append(tr, "b") // 2 2
	fmt.Println(len(tr), cap(tr))
	tr = append(tr, "c") // 3 4
	fmt.Println(len(tr), cap(tr))
```

  

Я так и не понял, но наверное make с map’ами раскрывается хорошо. пока я только понял что capacity до 256 в таком порядке будет увеличиваться в 2х то есть добавим ещё два элемента к коду выше capacity будет 8. а если мы условно добавим 5 (то есть у нас в целом элементов) то просто будет удваивать пока не уместится. То есть условно, у меня был список из трёх элементов я к ним прибавляю 6 и вроде бы кажется ну всё **capacity - 9, но факту она будет 10. ,**



## Есть ещё кстати следующий способ вывести массив:  
  

```Go
	for index := range a {
		sum += a[index]
	}
```

```Go
	for _, value := range array {
		sum += value
	}
```

если индекс нахуй не нужен

```go
	for index := range a {
		sum += a[index]
	}
```

если нужен ток индекс

  