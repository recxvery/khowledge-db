Это пиздец. ЕБАННЫЙ ГОЛЕНГ БЛЯТЬ


## Что будет если исключить обработку ошибок?

```go
func main() {
	file, _ := os.Open("somefile.txt")
	// if err != nil {
	// 	fmt.Println("Ошибка в открытии файла")
	// } //представим что у нас нет этого кода
	fmt.Println(file) // - value: nil

	file.Read("somefile.txt") //panic'a так как пытаемся вызвать метод на nil-pointer
}
```

# Error

Короче тут нету классической обработки ошибок как в других языках по типу try-except, здесь надо создавать её 🌈самому🌈

  

Пример базовичоK:

```Go
func divide(lhs int, rhs int) (int, error) {
	if rhs == 0 {
		return 0, erros.New("CANNOT DIVIDE BY ZERO")
	} else {
		return rhs / lhs , nil
	}
}
```

  
Пример посложнее:  

```Go
func calculateIMT(userHeight,userWeight float64, IMTPower int) (float64, error) {
	if userWeight <= 0 || userHeight  <= 0 {
		return 0, errors.New("НЕ УКАЗАН ВЕС ИЛИ РОСТ")
	}
	IMT := userWeight / math.Pow((userHeight / 100), float64(IMTPower))
	return IMT, nil
}
```

Чо происходит в этом круговороте пиздеца, сначала прописываем в calculateIMT переменную ошибку, создаём её с помощь erros.New() если нам нужно её передать, если не нужно как в конце то просто ставим **nil(это маркер когда мы не хотим возвращать какое-либо значение, либо оно равно 0, короче без мусора).**

```Go
func main() {
	fmt.Println("__ Калькулятор ИМТ __")
	for {
		userWeight,userHeight := getUserInput()
		IMT, err := calculateIMT(userHeight, userWeight, IMTPower)
		if err != nil {
			fmt.Println(err)
			continue
		}
		outputResult(IMT)
		isRepeatCalculation := getUserChoice() 
		if !isRepeatCalculation == false {
			break
		}
	}
}
```

тут всё наглядно. если у нас есть ошибка то выписываем её и скипаем итерацию цикла, всё изи вродь ёпт.

  

Если вайб есть заигнорить ебанную ошибку то:

```Go
func main() {
	fmt.Println("__ Калькулятор ИМТ __")
	for {
		userWeight,userHeight := getUserInput()
		IMT, _ := calculateIMT(userHeight, userWeight, IMTPower)
		// if err != nil {
		// 	fmt.Println(err)
		// 	continue
		// }
		outputResult(IMT)
		isRepeatCalculation := getUserChoice() 
		if !isRepeatCalculation == false {
			break
		}
	}
}
```

  

## **Panic**

это типо как error, только останавливает всё приложение и непосредсвтенно указывает на ошибку, в каком месте и пр. короче очень похоже на тесты с хекслета.

```Go

func main() {
	fmt.Println("__ Калькулятор ИМТ __")
	for {
		userHeight,userWeight := getUserInput()
		IMT, err  := calculateIMT(userHeight, userWeight, IMTPower)
		if err != nil {
			panic("Не заданы параметры для расчёта")
		}
		outputResult(IMT)
		isRepeatCalculation := getUserChoice() 
		if !isRepeatCalculation {
			break
		}
	}
}
```

её стоит использовать только в крайних случаях

  

## **Log.error**

Это как fmt.Print(”текст”) только ещё добавляет os.exit() в конце чтобы программа плавно завершилась.

```Go
    if err != nil {
        log.Fatal("Ошибка при маршалинге тела запроса:", err)
    }
```

https://purpleschool.ru/knowledge-base/article/error_handling



### Custom errors

```go
type constantStringErr string

func (e constantStringErr) Error() string {
	return string(e)
}

type constantIntErr int 

func (ei constantIntErr) Error() string {
	return fmt.Sprintf("error in code. Code of error [%d]", ei)
}

const (
	errFirst constantIntErr = iota
	errSecond 
)

func main() {

}

```

	Для чего они нам нахуй вообще нужны? 