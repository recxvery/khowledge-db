				[[Массивы в GO]]
[[Структуры]]
[[Функции]]

![[Pasted image 20250619025727.png]]
переменная number

```go
    number := 10
```

 ![[Pasted image 20250619025908.png]]
создали указатель

```go
    number := 10

    p := &number
```

==УКАЗАТЕЛЬ - ПЕРЕМЕННАЯ С АДРЕСОМ ДРУГОЙ ПЕРЕМЕННОЙ, С ПОМОЩЬЮ КОТОРОЙ МЫ СМОЖЕМ ПОЛУЧАТЬ ДОСТУП К ПЕРЕМЕННОЙ==

Окей, что в итоге происходит с этим кодом.
```go
package main

import "fmt"

func main() {
	number := 10

	
	p := &number
	
	// fmt.Println(p)
	
	foo(p) //10
	// boo(p)

	fmt.Println(number) //15

	fmt.Println("Разименование pointer:", *p) //15
}

func foo(num *int) {

	fmt.Println(*num)

	*num = 15
}
```

Мы создали переменную number куда передали значение 10. Далее мы создали указатель **p** который ссылается на эту переменную. В функции когда мы передаём указатель мы на самом деле создаём новый(то есть в функции как бы копия указателя, указывающее на тот же адрес переменной, что и оригинальный указатель). Таким образом, мы разыменуем новый указатель и меняем переменную **num**. Вуаля.

![[Pasted image 20250619031855.png]]
Короче, на мой взгляд указатели - указывают на определённую ячейку в памяти и помогают работать/преобразовать её не меняя место в оперативной памяти. Многим даётся это трудно понять, но учитывая что я уже 10 000ый раз застреваю на массивах я понял о чём идёт речь. 

**UPD:  указатели помогают влиять на объект, а не копировать его из раза и в раз и тем самым мы экономим огромное количество памяти.**

```go
package main

import (
	"fmt"
)

var msg string

func main() {
	msg := "Я скоро стану ниндзя!"	//0xc0000260a0
	fmt.Println(&msg)
	fmt.Println(msg)  //Я скоро стану ниндзя!
	changeMessage(&msg)

	fmt.Println(&msg)
	fmt.Println(msg)  //Я скоро стану ниндзя! (из функции changeMessage())
}

func changeMessage(msg *string) { //0xc0000260a0
	*msg += " (из функции changeMessage())" 
}
```

В общем. Тут мы просто видим. Так называемый referencing/dereferncing. На примере того что. Вот у нас есть переменная msg:

```go
	changeMessage(&msg) // здесь мы референсим то есть соединяем значение переменной и ячейку памяти
```


```go
func changeMessage(msg *string) { //0xc0000260a0
	*msg += " (из функции changeMessage())"
} //а здесь наоборот дерефенсирим то есть как бы из ячейки памяти вытаскиваем значение переменной.

	//то ЕСТЬ. * - dereference, & - reference
```


**КОРОЧЕ. 
![[Pasted image 20250616022210.png]]

Вот такую ещё отличную альтернативу привёл чат гпт:

📌 `&x` — "взять адрес x"  
📌 `*p` — "пойти по адресу p и достать значение". "разминовываем указатель"


![[Pasted image 20250815053400.png]]

![[Pasted image 20250815053601.png]]

![[Pasted image 20250815053611.png]]


Вот ещё как можно изменить значение с помощью указателя наглядно.

```go
package main

import (
	"fmt"
)

var (
	number = 5
	p *int // declare pointer
)

func main() {
	p = &number //reference
	fmt.Println(p) //0xee3320
	fmt.Println(&number) //0xee3320 

	*p += 50 //меняем number через указатель так как достаём значение из указателя. тут грубо говоря dereference.

	fmt.Println(number)
}``` ![[Pasted image 20250815053354.png]]