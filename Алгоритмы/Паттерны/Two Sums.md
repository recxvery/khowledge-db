
## Почти решил сам.

 Проблемный код: 
```go
 package main

import "fmt"

func main() {
	nums := []int{3, 2, 3}
	target := 6
	(twoSums(nums, target))
}

func twoSums(array []int, target int)  {
	m := make(map[int]int)
	for i, v := range array{
		m[i] = target - v  // 0 : 3, 1 : 2, 2 : 3 //cвернул еще здесь не туда
		//нужно было вместо того чтобы создавать цикл ещё один. просто писать
		// _, ok := m[target - v] без цикла
	}
	fmt.Println(m)
	for i, v  := range m {
		// fmt.Println(i)
		t1 := target - v // 6 - 3 = 3
		// fmt.Println(i, t1)
		i2, ok := m[t1]
		fmt.Println(ok)
		if ok {
			fmt.Println(i, i2)
		}
	}
   //этот цикл очевидно тоже не нужен
}

```

Главной сложностью было не понять паттерн(я его понял). А придумать как положить в мапу ключи с чем у меня произошли большие сложности. Ключевой паттерн - Two Sums - Hash map. Оказывается можно просто создавать сразу ключ на месте а не вводить новый цикл и уже по этому ключу искать нужный нам элемент.

#### Паттерн

Когда нужно отследить/отсортировать/найти индекс/ значение в массиве по какому-либо элементу, мы просто используем мапу.